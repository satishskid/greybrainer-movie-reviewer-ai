# Gemini API Key (BYOK - Bring Your Own Key system)
# Users will be prompted to enter their Gemini API key via the application interface
# Get from: https://aistudio.google.com/app/apikey
# Note: No API keys need to be configured in environment variables
# All API keys are managed securely through the BYOK system

# Firebase configuration is now in src/firebase.ts
